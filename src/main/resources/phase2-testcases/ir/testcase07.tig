void main() begin
  begin
    var i, x : int := 4;

    while ((x <> 10) & (x < 5)) do
      x := x + 1;
    enddo;
    x := 10;

    for i := 0 to 5 do
      if ((x = 5) | (x < 3)) then
          x := x - 1;
      endif;
    enddo;
    x := 5;
  end;
end;
